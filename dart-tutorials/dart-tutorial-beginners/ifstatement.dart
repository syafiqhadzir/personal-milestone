void main() {
  bool isSmart = true;
  bool isStudent = false;

  // String greeting = "Hello World";
  // print(greeting.contains("e"));

  // && and
  // || or
  //  ! negate

  if (isSmart && !isStudent) print("You are smart but not a student.");
}
