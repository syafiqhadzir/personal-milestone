**Arrow Function Syntax () => {}**

- Parameters

  ```javascript
  () => { ... }   // no parameter
  a => { ... }    // single parameter
  (a, b) => { ... } // several parameters
  ```

- Body

  ```javascript
  a => { ... } // several statements
  a => a * a // single statement
  ```
